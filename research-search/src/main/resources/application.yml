server:
  port: 8087

spring:
  application:
    name: research-search
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
        namespace: research-platform
        group: DEFAULT_GROUP
        register-enabled: true
        fail-fast: false

jwt:
  secret: project-secret-key-1234567890
  expire: 7200

logging:
  level:
    com.research.search: debug
